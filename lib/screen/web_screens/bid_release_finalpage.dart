import 'package:flutter/material.dart';

class BidSubmissionfinalPage extends StatelessWidget {
  const BidSubmissionfinalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: TextButton(onPressed: (){}, child: Text("Submit Bid")),
    );
  }
}